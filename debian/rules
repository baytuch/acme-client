#!/usr/bin/make -f
# See debhelper(7) (uncomment to enable)
export DH_OPTIONS
export DH_VERBOSE=1

%:
	dh $@ --with autoreconf

override_dh_auto_configure:
	dh_auto_configure -- \
		--prefix=/usr

override_dh_install:
	dh_install
